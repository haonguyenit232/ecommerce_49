 <% provide :title, t(".page_title")%>

 <div class="panel panel-default panel-table">
  <div class="panel-heading">
    <div class="row">
      <div class="col col-xs-6">
        <h3 class="panel-title"><%= t ".panel_title" %></h3>
         <%= link_to "Export data(.csv)", admin_products_path(format: "csv"), class: "float-right btn btn-primary", style: "margin-left:10px"%>
         <!--btn click =>  file_field_tag
         click . file_field_tag chon xong la submit doan onclick ni xoa dc a hay co js mo.
        cai' nay` anh luoi nua @@!. khi em vao` trang nay` lenh javascrip nay` se tim tat' ca file_fied, neu' co 2 file fied thi` se bi loi-->
        <button id="btn-import-csv" class="btn btn-primary" onclick="javascript:document.getElementById('file_option').click();">Import data(.csv)</button>
        <div class="invisible">
          <%= form_tag(import_admin_products_path, multipart: true) do %>
            <%= file_field_tag :file, accpet: 'text/csv', onchange: "javascript:this.form.submit();", id: "file_option" %>
          <% end %>
        </div>
      </div>
    </div>
    <div class="row">
      <div class="col-md-6">
        <%= will_paginate @products %>
      </div>
    </div>
  </div>
  <div class="panel-body">
    <table class="table table-striped table-bordered table-list">
      <thead>
        <tr>
          <th><%= t "product_image" %></th>
          <th><%= t ".name" %></th>
          <th><%= t ".price" %></th>
          <th><%= t ".quantity" %></th>
          <th><%= t "category" %></th>
          <th><em class="fa fa-cog"></em></th>
        </tr>
      </thead>
      <tbody>
        <%= render partial: "product", collection: @products, as: :product %>
      </tbody>
    </table>

  </div>
  <div class="panel-footer">
    <div class="row">
      <div class="col col-xs-4">
        <%= page_entries_info @products %>
      </div>
      <div class="col col-xs-8">
        <%= will_paginate @products %>
      </div>
    </div>
  </div>
</div>
